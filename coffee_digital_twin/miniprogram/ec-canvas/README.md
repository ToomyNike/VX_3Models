Place the official echarts-for-weixin ec-canvas component in this folder when chart rendering is upgraded.

The MVP pages currently use components/chart-card as a stable fallback so the demo can run before ECharts files are copied in.
