# 答辩问题准备

## 为什么采用微信小程序？

果农使用成本低，免安装，适合田间地头快速录入农事数据。

## 为什么用 Flask？

MVP 阶段接口少、部署轻，Flask 便于快速把小程序请求、SQLite 和模型 adapter 串起来。

## 三模型如何协同？

APSIM 负责生育期、产量和水肥胁迫，HYDRUS 解释土壤水分和根系吸水，BEPS 扩展冠层生产力和生态指标，后端统一融合为小程序可展示的 JSON。

## 演示时模型不可用怎么办？

adapter 保留标准 JSON 兜底输出，确保演示流程不断链。
